# Copy to .env.local for local testing.
# Never commit real tokens.

GROQ_API_KEY=

# Public app origin (Coolify). Used for Discord hints + branded source redirects.
# Discord Interactions require HTTPS — put TLS in front of Coolify, then set this to https://...
PUBLIC_APP_URL=http://uln4n0vf3xlwibas8o3iowno.146.59.93.94.sslip.io

# Discord Ask (primary chat channel — see DISCORD_SETUP.md)
DISCORD_PUBLIC_KEY=
DISCORD_BOT_TOKEN=
DISCORD_APPLICATION_ID=
# Optional: comma-separated guild IDs (empty = allow all)
DISCORD_ALLOWED_GUILD_IDS=

# Meta WhatsApp Cloud API (optional / legacy)
WHATSAPP_VERIFY_TOKEN=newsdash-verify-change-me
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_APP_SECRET=
WHATSAPP_WABA_ID=

# Persistent Chat Memory (Upstash Redis — sign up at upstash.com)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Hybrid RAG (Upstash Vector — create a HYBRID index with BGE-M3 dense + sparse)
# Console: https://console.upstash.com/vector
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=
